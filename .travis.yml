language: python
python:
    - "2.7"
    - "3.5"
    - "3.6"
install: 
    - "pip install -r requirements.txt --use-mirrors"
    - "pip install pytest mock --use-mirrors"
    - "pip install ."
script: 
    - "pytest test"
